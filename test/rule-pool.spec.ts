import { strict as assert } from 'assert';

describe('RulePool', () => {
    describe('temp-1', () => {
        it('temp-2', () => {
            assert.equal(2 === parseInt('2'), true);
        });
    });
});
